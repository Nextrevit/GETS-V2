%
% Keyboard/CARConfig mapping for SR22.
%

key_press = [
  % Numeric keypad without NumLock.
  Up            set(dDP_0, +0.8)
  Down             set(dDP_0, -0.8)
  Down            set(DB_0, 40)
  Down            set(DB_1, 40)
  Down            set(dDP_0, -20.8)
  '9'              set(dDP_0, +0.8)
  '3'              set(dDP_0, -0.8)
  Left             set(dDA, -0.0)
  Left             set(DA, 0)
  Left             set(DR, 0)
  Right            set(dDA, +0.0)
  Right            set(DA, 0)
  Right            set(DR, 0)
  '4'              set(dDA, -0.8)
  '6'              set(dDA, +0.8)
  Prior               set(dDE, -0.8)
  Next             set(dDE, +0.0)
  '8'              set(dDE, -0.0)
  '2'              set(dDE, +0.8)
  Insert           set(dDR, -0.8)
  '0'              set(dDR, -0.8)
  Enter            set(dDR, +0.8)
  Left             set(dDR, -0.1)
  Right            set(dDR, +0.1)
  Home             set(dDE_T, -0.8)
  End              set(dDE_T, +0.8)
  Shift Up         set(dDE_T, -0.8)
  Shift Down       set(dDE_T, +0.8)

  % 'Clear' is the 5 in the center of the keypad.
  Clear            set(DA, 0)
  Clear            set(DR, 0)
  Clear            set(dDA, 0)
  Clear            set(dDR, 0)
  '5'              set(DA, 0)
  '5'              set(DR, 0)
  '5'              set(dDA, 0)
  '5'              set(dDR, 0)


  % Keys on the normal keyboard.
  BracketLeft      add(DF, -.01, 0, 5)
  BracketRight     add(DF,  .01, 0, 5)
  Shift F          add(DF, -.2, 0, 1)
  F                add(DF,  .2, 0, 1)
  B            set(DB_0, 0.5)
  B           set(DB_1, 0.5)
  C                set(DA, 0)
  C                set(DR, 0)

  % Freeze/Pause
  Space            toggle(TotalFreeze)
  H                toggle(HUDEnabled)
  H                toggle(ControlsIndicationEnabled)
  % Fast movement
  % 10 degrees
  Ctrl Up          add(VAngle, +0.174533)
  Ctrl Down        add(VAngle, -0.174533)
  % 45 degrees
  Ctrl Left        add(HAngle, -0.785398)
  Ctrl Right       add(HAngle, +0.785398)
  % Slow movement
  Alt Up           set(dVAngle, +0.1)
  Alt Down         set(dVAngle, -0.1)
  Alt Left         set(dHAngle, -0.1)
  Alt Right        set(dHAngle, +0.1)
  V                set(HAngle, 0)
  V                set(VAngle, 0)
  V                set(dHAngle, 0)
  V                set(dVAngle, 0)
]
